import { HeroSection } from "@/components/home/HeroSection";

export default function Home() {
  return (
    <div className="min-h-screen">
      <HeroSection />
      {/* Add more sections here if needed for the home page, e.g. Selected Works preview */}
    </div>
  );
}
